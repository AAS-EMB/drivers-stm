#include <gtest/gtest.h>
#include "timer.hpp"

// TEST(SystemClock, Monotonic) {
//     using clock = driver::system_clock;

//     clock::init();
//     auto t1 = clock::now();
//     auto t2 = clock::now();

//     EXPECT_LE(t1, t2);
// }